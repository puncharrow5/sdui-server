// 관리자
model Admin {
    id       Int    @id @default(autoincrement())
    email    String
    password String

    // 사이트
    siteId Int?
    Site   Site? @relation(fields: [siteId], references: [id])
}
