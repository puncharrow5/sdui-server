// 사이트
model Site {
    id     Int    @id @default(autoincrement())
    domain String @unique
    name   String
    email  String

    // 헤더
    Header Header?

    // 컴포넌트
    compoents Component[]

    // 관리자
    admins SiteAdmin[]
}
