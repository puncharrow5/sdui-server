// 헤더
model Header {
    id              Int     @id @default(autoincrement())
    logo            String?
    backgroundColor String?
    textSize        String?
    textColor       String?

    // 사이트
    siteId Int?  @unique
    site   Site? @relation(fields: [siteId], references: [id])
}
